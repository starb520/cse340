
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      
    } %>


<% if (message) { %>
    <p class="notice"><%= message %></p>
    <% } %>

<a href="/inv/add-classification"> Add New Classification </a>
<br>
<a href="url">Add New Vehicle </a>